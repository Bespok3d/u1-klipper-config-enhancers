# Changelog

## 0.1.0

- First release. Overrides Klipper's idle timeout with a value you choose in seconds (default 7200,
  two hours), so the printer stays awake longer than the stock 5 minute default. Applied as an
  [idle_timeout] config override so it sticks across prints. Snapmaker U1 verified.
